{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/form.css') }}">
{% endblock %}

<div class="container">
{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
    <button class="btn btn-outline-success rounded-pill px-4 py-2 shadow-sm">{{ button_label|default('sauvegarder') }}</button>
{{ form_end(form) }}
</div>